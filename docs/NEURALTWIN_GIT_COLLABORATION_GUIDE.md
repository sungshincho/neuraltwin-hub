# NEURALTWIN 듀얼 챗봇 Git 협업 가이드

> **버전**: 1.0
> **작성일**: 2026-02-05
> **프로젝트**: NEURALTWIN Customer Dashboard
> **대상**: 개발자 A(UI/UX), B(Web Bot), C(OS Bot)

---

## 목차

1. [핵심 원칙](#1-핵심-원칙)
2. [브랜치 전략](#2-브랜치-전략)
3. [파일 소유권 맵](#3-파일-소유권-맵)
4. [A + B 웹사이트 챗봇 동시 작업](#4-a--b-웹사이트-챗봇-동시-작업)
5. [A + C OS 챗봇 동시 작업](#5-a--c-os-챗봇-동시-작업)
6. [인터페이스 계약 기반 병렬 개발](#6-인터페이스-계약-기반-병렬-개발)
7. [PR 워크플로우 & 코드 리뷰](#7-pr-워크플로우--코드-리뷰)
8. [충돌 방지 실전 규칙](#8-충돌-방지-실전-규칙)
9. [일일 동기화 체크리스트](#9-일일-동기화-체크리스트)
10. [부록: 빠른 참조 카드](#10-부록-빠른-참조-카드)

---

## 1. 핵심 원칙

### 1.1 "같은 레포, 다른 디렉토리, 계약으로 연결"

3명의 개발자가 하나의 레포에서 두 개의 챗봇을 동시에 개발합니다. 충돌 없이 병렬 작업하기 위해 다음 원칙을 따릅니다:

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    NEURALTWIN Customer Dashboard                        │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    공유 계약 레이어 (🟪 전원 합의)                  │  │
│  │                                                                   │  │
│  │  src/shared/chat/types/chat.types.ts (신규)                       │  │
│  │  src/types/auth.ts (기존)                                         │  │
│  │  supabase/migrations/*_chat_schema.sql (신규)                     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌─────────────────────────┐     ┌─────────────────────────────────┐   │
│  │   웹사이트 챗봇 영역      │     │       OS 챗봇 영역               │   │
│  │                         │     │                                 │   │
│  │  🟦 UI (A 전담)         │     │  🟦 UI (A 전담)                 │   │
│  │  src/features/          │     │  src/features/                  │   │
│  │    website-chatbot/     │     │    ai-assistant/                │   │
│  │    (신규)               │     │    (신규)                       │   │
│  │                         │     │                                 │   │
│  │  🟩 Backend (B 전담)    │     │  🟧 Backend (C 전담)            │   │
│  │  supabase/functions/    │     │  supabase/functions/            │   │
│  │    retail-chatbot/      │     │    neuraltwin-assistant/        │   │
│  │    (신규)               │     │    (신규)                       │   │
│  └─────────────────────────┘     └─────────────────────────────────┘   │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐  │
│  │                    기존 인프라 (변경 최소화)                        │  │
│  │                                                                   │  │
│  │  src/components/ui/          (기존 shadcn/ui)                     │  │
│  │  src/components/features/    (기존 피처 컴포넌트)                   │  │
│  │  src/hooks/                  (기존 훅)                             │  │
│  │  src/pages/                  (기존 페이지)                          │  │
│  │  supabase/functions/submit-contact/  (기존 EF)                     │  │
│  │  supabase/functions/upscale-image/   (기존 EF)                     │  │
│  └───────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 1.2 프로젝트 구조 분석 결과

현재 프로젝트 분석을 통해 확인된 구조:

| 항목 | 현재 상태 | 챗봇 개발 대응 |
|:---|:---|:---|
| `src/shared/` | **없음** | 신규 생성 (`src/shared/chat/`) |
| `src/features/` | **없음** (대신 `src/components/features/`가 flat 구조로 존재) | 신규 생성 (`src/features/website-chatbot/`, `src/features/ai-assistant/`) |
| `supabase/functions/_shared/` | **없음** | 신규 생성 |
| Zustand store | **없음** | OS 챗봇용 신규 생성 필요 |
| develop 브랜치 | **없음** (main만 존재) | 신규 생성 |
| 커밋 컨벤션 | **혼재** (feat: + 일반 문장) | 새 컨벤션 도입 |
| 타입 파일 | `src/types/` 존재 | 기존 패턴 따르기 |
| 라우팅 | `src/App.tsx`에서 직접 정의 | 라우트 목록 참조 |
| 기존 EF 패턴 | `index.ts` 단일 파일, `serve()` 함수 | 동일 패턴 적용 |

### 1.3 세 가지 불변 규칙

```
┌────────────────────────────────────────────────────────────────────────┐
│  규칙 1: 🚫 남의 영역 코드 직접 수정 금지                                │
│         → 인터페이스 변경은 Slack 알림 → 합의 → PR 순서로               │
├────────────────────────────────────────────────────────────────────────┤
│  규칙 2: 📝 계약 파일 수정 시 전원 알림 필수                             │
│         → chat.types.ts, assistant.types.ts, website.types.ts         │
├────────────────────────────────────────────────────────────────────────┤
│  규칙 3: 🔄 매일 아침 develop에서 rebase                                │
│         → git fetch origin && git rebase origin/develop               │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 브랜치 전략

### 2.1 현재 Git 상태

```bash
# 현재 브랜치 현황
$ git branch -a
* main
  remotes/origin/main
```

현재 `main` 브랜치만 존재합니다. 듀얼 챗봇 개발을 위해 `develop` 브랜치를 신규 생성합니다.

### 2.2 브랜치 구조 설정

```bash
# Step 1: develop 브랜치 생성 (최초 1회, 팀 리드가 실행)
git checkout main
git pull origin main
git checkout -b develop
git push -u origin develop

# Step 2: GitHub에서 develop을 default 브랜치로 설정
# Settings → Branches → Default branch → develop 선택
```

### 2.3 브랜치 네이밍 규칙

```
feat/{담당자코드}-{기능설명}

담당자 코드:
  A → ui    (UI/UX 담당)
  B → web   (웹사이트 챗봇 담당)
  C → os    (OS 챗봇 담당)

예시:
  feat/ui-shared-chat-components     ← A: 공유 Chat UI Kit
  feat/ui-website-chatbot-widget     ← A: 웹사이트 챗봇 위젯
  feat/ui-assistant-panel            ← A: OS 챗봇 패널
  feat/web-retail-chatbot-ef         ← B: retail-chatbot EF
  feat/web-lead-capture              ← B: 리드 캡처 기능
  feat/os-assistant-ef               ← C: neuraltwin-assistant EF
  feat/os-intent-classifier          ← C: 인텐트 분류기
```

### 2.4 브랜치 흐름

```
main (프로덕션)
  │
  └── develop (통합 브랜치)
       │
       ├── feat/ui-shared-chat-components     ← A
       ├── feat/ui-website-chatbot-widget     ← A
       ├── feat/ui-assistant-panel            ← A
       │
       ├── feat/web-retail-chatbot-ef         ← B
       ├── feat/web-lead-capture              ← B
       │
       ├── feat/os-assistant-ef               ← C
       ├── feat/os-intent-classifier          ← C
       │
       └── schema/chat-tables                 ← B+C 공동
```

### 2.5 Merge 규칙

| 방향 | Merge 방식 | 이유 |
|:---|:---|:---|
| `feat/*` → `develop` | **Squash Merge** | 커밋 히스토리 정리, 하나의 기능 = 하나의 커밋 |
| `develop` → `main` | **Merge Commit** | 릴리즈 히스토리 보존 |

```bash
# feature → develop (Squash Merge)
git checkout develop
git merge --squash feat/ui-shared-chat-components
git commit -m "feat(shared): Chat UI Kit 컴포넌트 8개 추가"
git push origin develop

# develop → main (Merge Commit, 릴리즈 시)
git checkout main
git merge develop --no-ff -m "release: 듀얼 챗봇 v1.0"
git push origin main
```

---

## 3. 파일 소유권 맵

### 3.1 소유권 범례

```
🟦 개발자 A (UI/UX)     — 프론트엔드 UI 컴포넌트
🟩 개발자 B (Web Bot)   — 웹사이트 챗봇 백엔드
🟧 개발자 C (OS Bot)    — OS 챗봇 백엔드
🟪 전원 합의 필요       — 계약 파일, 공유 타입, DB 스키마
⬜ 기존 코드 (수정 최소화)
```

### 3.2 전체 프로젝트 소유권 트리

```
neuraltwin-hub/
│
├── src/
│   │
│   ├── shared/                              (신규 디렉토리)
│   │   └── chat/                            🟦 A 소유
│   │       ├── components/
│   │       │   ├── ChatBubble.tsx           🟦
│   │       │   ├── ChatInput.tsx            🟦
│   │       │   ├── TypingIndicator.tsx      🟦
│   │       │   ├── SuggestionChips.tsx      🟦
│   │       │   ├── FeedbackButtons.tsx      🟦
│   │       │   ├── ChatScrollArea.tsx       🟦
│   │       │   ├── WelcomeMessage.tsx       🟦
│   │       │   └── MarkdownRenderer.tsx     🟦
│   │       ├── hooks/
│   │       │   ├── useStreaming.ts          🟦
│   │       │   ├── useChatSession.ts        🟦
│   │       │   └── useFeedback.ts           🟦
│   │       └── types/
│   │           └── chat.types.ts            🟪 전원 합의
│   │
│   ├── features/                            (신규 디렉토리)
│   │   │
│   │   ├── website-chatbot/                 (신규)
│   │   │   ├── ChatbotWidget.tsx            🟦 A
│   │   │   ├── ChatbotTrigger.tsx           🟦 A
│   │   │   ├── LeadCaptureForm.tsx          🟦 A
│   │   │   ├── hooks/
│   │   │   │   └── useChatbot.ts            🟩 B
│   │   │   └── types/
│   │   │       └── website.types.ts         🟩 B
│   │   │
│   │   └── ai-assistant/                    (신규)
│   │       ├── AssistantPanel.tsx           🟦 A
│   │       ├── AssistantButton.tsx          🟦 A
│   │       ├── components/
│   │       │   ├── InlineChart.tsx          🟦 A
│   │       │   ├── ActionButtons.tsx        🟦 A
│   │       │   └── QuickActions.tsx         🟦 A
│   │       ├── hooks/
│   │       │   ├── useAssistant.ts          🟧 C
│   │       │   └── useActionDispatch.ts     🟧 C
│   │       ├── providers/
│   │       │   └── AssistantProvider.tsx    🟧 C
│   │       ├── store/
│   │       │   └── assistantStore.ts        🟧 C (신규)
│   │       └── types/
│   │           └── assistant.types.ts       🟧 C
│   │
│   ├── components/                          ⬜ 기존 (수정 최소화)
│   │   ├── ui/                              ⬜ shadcn/ui 컴포넌트
│   │   ├── features/                        ⬜ 기존 피처 컴포넌트
│   │   │   ├── TrafficHeatmap.tsx
│   │   │   ├── Store3DViewer.tsx
│   │   │   ├── LayoutSimulator.tsx
│   │   │   └── ... (기타 기존 컴포넌트)
│   │   └── layout/                          ⬜ Header, Footer
│   │
│   ├── hooks/                               ⬜ 기존 (수정 최소화)
│   │   ├── useAuth.ts                       ⬜
│   │   ├── use-toast.ts                     ⬜
│   │   └── use-mobile.tsx                   ⬜
│   │
│   ├── pages/                               ⬜ 기존 (필요시 A가 수정)
│   │   ├── Index.tsx                        ⬜ (웹 챗봇 위젯 통합 시 A 수정)
│   │   ├── Dashboard.tsx                    ⬜ (OS 챗봇 패널 통합 시 A 수정)
│   │   └── ... (기타 페이지)
│   │
│   ├── types/                               ⬜ 기존 (확장 시 합의)
│   │   └── auth.ts                          ⬜
│   │
│   ├── App.tsx                              🟪 전원 합의 (라우트 추가 시)
│   └── main.tsx                             ⬜ 기존
│
├── supabase/
│   ├── functions/
│   │   │
│   │   ├── _shared/                         (신규 디렉토리)
│   │   │   ├── chatLogger.ts                🟧 C
│   │   │   ├── streamingResponse.ts         🟧 C
│   │   │   ├── retailGlossary.ts            🟧 C
│   │   │   ├── rateLimiter.ts               🟧 C
│   │   │   ├── errorHandler.ts              🟧 C
│   │   │   └── chatTypes.ts                 🟪 전원 합의
│   │   │
│   │   ├── retail-chatbot/                  (신규)
│   │   │   ├── index.ts                     🟩 B
│   │   │   ├── prompts/
│   │   │   │   └── retailExpert.ts          🟩 B
│   │   │   ├── salesBridge.ts               🟩 B
│   │   │   ├── painPointExtractor.ts        🟩 B
│   │   │   └── leadCapture.ts               🟩 B
│   │   │
│   │   ├── neuraltwin-assistant/            (신규)
│   │   │   ├── index.ts                     🟧 C
│   │   │   ├── intent/
│   │   │   │   ├── classifier.ts            🟧 C
│   │   │   │   ├── entityExtractor.ts       🟧 C
│   │   │   │   └── patterns.ts              🟧 C
│   │   │   ├── actions/
│   │   │   │   ├── queryActions.ts          🟧 C
│   │   │   │   ├── aiActions.ts             🟧 C
│   │   │   │   └── uiActions.ts             🟧 C
│   │   │   ├── orchestrator/
│   │   │   │   └── functionCaller.ts        🟧 C
│   │   │   └── response/
│   │   │       ├── generator.ts             🟧 C
│   │   │       └── suggester.ts             🟧 C
│   │   │
│   │   ├── submit-contact/                  ⬜ 기존 EF
│   │   │   └── index.ts
│   │   │
│   │   └── upscale-image/                   ⬜ 기존 EF
│   │       └── index.ts
│   │
│   ├── migrations/                          (신규 파일 추가)
│   │   └── YYYYMMDD_chat_schema.sql         🟪 B+C 공동
│   │
│   └── config.toml                          ⬜ 기존
│
├── .github/                                 (신규 디렉토리)
│   ├── CODEOWNERS                           🟪 전원 합의
│   └── pull_request_template.md             🟪 전원 합의
│
├── package.json                             🟪 전원 합의
├── tsconfig.json                            🟪 전원 합의
└── ... (기타 설정 파일)
```

### 3.3 CODEOWNERS 파일

`.github/CODEOWNERS` 파일을 생성합니다:

```
# ===================================================================
# NEURALTWIN 듀얼 챗봇 CODEOWNERS
# ===================================================================
#
# 개발자 매핑 (GitHub 사용자명으로 교체 필요):
#   @developer-a  → UI/UX 담당
#   @developer-b  → 웹사이트 챗봇 담당
#   @developer-c  → OS 챗봇 담당
#
# ===================================================================

# ─────────────────────────────────────────────────────────────────────
# 🟪 전원 합의 필요 영역 (CRITICAL)
# ─────────────────────────────────────────────────────────────────────

# 계약 타입 파일
src/shared/chat/types/                  @developer-a @developer-b @developer-c
src/features/*/types/                   @developer-a @developer-b @developer-c
supabase/functions/_shared/chatTypes.ts @developer-a @developer-b @developer-c

# DB 스키마
supabase/migrations/                    @developer-b @developer-c

# 핵심 설정 파일
package.json                            @developer-a @developer-b @developer-c
tsconfig.json                           @developer-a @developer-b @developer-c
src/App.tsx                             @developer-a @developer-b @developer-c

# ─────────────────────────────────────────────────────────────────────
# 🟦 개발자 A (UI/UX) 전담 영역
# ─────────────────────────────────────────────────────────────────────

# 공유 Chat UI Kit
src/shared/chat/components/             @developer-a
src/shared/chat/hooks/                  @developer-a

# 웹사이트 챗봇 UI
src/features/website-chatbot/*.tsx      @developer-a

# OS 챗봇 UI
src/features/ai-assistant/*.tsx         @developer-a
src/features/ai-assistant/components/   @developer-a

# ─────────────────────────────────────────────────────────────────────
# 🟩 개발자 B (Web Bot) 전담 영역
# ─────────────────────────────────────────────────────────────────────

# 웹사이트 챗봇 훅/타입
src/features/website-chatbot/hooks/     @developer-b
src/features/website-chatbot/types/     @developer-b

# 웹사이트 챗봇 Edge Function
supabase/functions/retail-chatbot/      @developer-b

# ─────────────────────────────────────────────────────────────────────
# 🟧 개발자 C (OS Bot) 전담 영역
# ─────────────────────────────────────────────────────────────────────

# OS 챗봇 훅/타입/Provider/Store
src/features/ai-assistant/hooks/        @developer-c
src/features/ai-assistant/types/        @developer-c
src/features/ai-assistant/providers/    @developer-c
src/features/ai-assistant/store/        @developer-c

# OS 챗봇 Edge Function
supabase/functions/neuraltwin-assistant/ @developer-c

# 공유 EF 유틸리티
supabase/functions/_shared/             @developer-c

# ─────────────────────────────────────────────────────────────────────
# ⬜ 기존 코드 (수정 시 주의)
# ─────────────────────────────────────────────────────────────────────

# 기존 컴포넌트/훅은 필요시 A가 수정 가능하나 신중하게
src/components/                         @developer-a
src/hooks/                              @developer-a
src/pages/                              @developer-a
```

---

## 4. A + B 웹사이트 챗봇 동시 작업

### 4.1 경계선 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    웹사이트 챗봇 (NEURAL) 아키텍처                        │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │                     🟦 A 영역 (UI/UX)                        │       │
│  │                                                             │       │
│  │  src/shared/chat/           src/features/website-chatbot/   │       │
│  │  ├── components/            ├── ChatbotWidget.tsx           │       │
│  │  │   ├── ChatBubble.tsx     ├── ChatbotTrigger.tsx          │       │
│  │  │   ├── ChatInput.tsx      └── LeadCaptureForm.tsx         │       │
│  │  │   └── ...                                                │       │
│  │  └── hooks/                                                 │       │
│  │      └── useStreaming.ts ←──┐                               │       │
│  └──────────────────────────────│───────────────────────────────┘       │
│                                 │                                       │
│  ════════════════════════════════════════════════════════════════       │
│                     🟪 계약 경계선 (인터페이스)                          │
│                                 │                                       │
│     WebChatRequest ────────────→│                                       │
│     WebChatResponse ←───────────│                                       │
│     SSE Event Stream ←──────────│                                       │
│  ════════════════════════════════════════════════════════════════       │
│                                 │                                       │
│  ┌──────────────────────────────│───────────────────────────────┐       │
│  │                     🟩 B 영역 (Backend)                      │       │
│  │                              │                               │       │
│  │  src/features/website-chatbot/                               │       │
│  │  ├── hooks/                                                  │       │
│  │  │   └── useChatbot.ts ─────┘ (API 호출 구현)                │       │
│  │  └── types/                                                  │       │
│  │      └── website.types.ts                                    │       │
│  │                                                              │       │
│  │  supabase/functions/retail-chatbot/                          │       │
│  │  ├── index.ts                                                │       │
│  │  ├── prompts/retailExpert.ts                                 │       │
│  │  ├── salesBridge.ts                                          │       │
│  │  ├── painPointExtractor.ts                                   │       │
│  │  └── leadCapture.ts                                          │       │
│  └──────────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────────────┘
```

### 4.2 병렬 개발 플로우

```
Phase 0 (Week 1-3)              Phase 1 (Week 4-6)              Phase 2 (Week 7)
┌───────────────────┐          ┌───────────────────┐          ┌───────────────────┐
│ 인터페이스 합의    │          │ Mock 기반 독립 개발 │          │    연동 통합       │
│                   │          │                   │          │                   │
│ A+B 공동:         │          │ A: UI 개발        │          │ A+B 공동:         │
│ - 요청/응답 타입   │    ──→   │    (Mock 사용)    │    ──→   │ - Mock → 실제 API  │
│ - SSE 이벤트 정의  │          │                   │          │ - E2E 테스트       │
│ - 스트리밍 계약    │          │ B: EF 개발        │          │ - 버그 픽스        │
│                   │          │    (curl 테스트)   │          │                   │
└───────────────────┘          └───────────────────┘          └───────────────────┘
```

### 4.3 인터페이스 계약서

#### WebChatRequest (A → B)

```typescript
// src/features/website-chatbot/types/website.types.ts

export interface WebChatRequest {
  message: string;
  sessionId: string;
  conversationId?: string;
  turnCount: number;
  metadata?: {
    utmSource?: string;
    referrer?: string;
    userAgent?: string;
  };
}
```

#### WebChatResponse (B → A)

```typescript
// src/features/website-chatbot/types/website.types.ts

export interface WebChatResponse {
  message: string;
  meta: {
    conversationId: string;
    topicCategory: string;
    painPoint?: string;
    sentiment: 'positive' | 'neutral' | 'negative';
    solutionMentioned: boolean;
  };
  suggestions?: string[];
  showLeadForm?: boolean;
}
```

### 4.4 SSE 스트리밍 이벤트 계약

```typescript
// SSE 이벤트 타입 (B가 전송, A가 파싱)

// 1. 텍스트 청크 (스트리밍 중)
event: chunk
data: {"text": "안녕하세요! 리테일 전문"}

// 2. 스트리밍 완료
event: done
data: {"message": "전체 응답 텍스트..."}

// 3. 메타데이터 (스트리밍 완료 후)
event: meta
data: {
  "conversationId": "uuid",
  "topicCategory": "layout",
  "painPoint": "동선 문제",
  "sentiment": "neutral",
  "solutionMentioned": false
}

// 4. 추천 질문
event: suggestions
data: ["전환율 개선 방법은?", "매장 레이아웃 최적화"]

// 5. 에러
event: error
data: {"code": "RATE_LIMIT", "message": "요청 한도 초과"}
```

### 4.5 Mock 기반 독립 개발

#### A가 사용할 Mock Hook

```typescript
// src/features/website-chatbot/hooks/useChatbot.mock.ts

import { useState, useCallback } from 'react';
import type { WebChatRequest, WebChatResponse } from '../types/website.types';

// Mock 응답 시나리오
const MOCK_RESPONSES: Record<string, Partial<WebChatResponse>> = {
  default: {
    message: '안녕하세요! 리테일 전문 AI NEURAL입니다. 매장 운영, 고객 동선, 전환율 개선 등 리테일 관련 질문에 답변해 드립니다.',
    suggestions: ['전환율 개선 방법은?', '매장 레이아웃 최적화', '리테일 KPI 벤치마크'],
    meta: {
      conversationId: 'mock-conv-001',
      topicCategory: 'general',
      sentiment: 'neutral',
      solutionMentioned: false,
    },
  },
  layout: {
    message: '매장 레이아웃은 고객 동선과 직결됩니다. 입구에서 3m는 "감속 구간"으로 상품 배치를 피하는 것이 좋습니다. 업계에서는 이를 Decompression Zone이라고 부릅니다.',
    suggestions: ['동선 최적화 방법은?', 'VMD 원칙 알려줘', '골든존이 뭐야?'],
    meta: {
      conversationId: 'mock-conv-001',
      topicCategory: 'layout',
      painPoint: '매장 레이아웃 고민',
      sentiment: 'neutral',
      solutionMentioned: false,
    },
  },
  leadCapture: {
    message: '말씀하신 매장 구조라면, 3D 디지털 트윈으로 재배치 효과를 미리 시뮬레이션해볼 수 있어요.',
    showLeadForm: true,
    meta: {
      conversationId: 'mock-conv-001',
      topicCategory: 'layout',
      sentiment: 'positive',
      solutionMentioned: true,
    },
  },
};

export function useChatbotMock() {
  const [messages, setMessages] = useState<Array<{ role: 'user' | 'assistant'; content: string }>>([]);
  const [isStreaming, setIsStreaming] = useState(false);
  const [streamingText, setStreamingText] = useState('');

  const sendMessage = useCallback(async (request: WebChatRequest): Promise<WebChatResponse> => {
    // Mock 응답 선택
    const scenario = request.message.includes('레이아웃') ? 'layout'
                   : request.turnCount > 5 ? 'leadCapture'
                   : 'default';
    const mockResponse = MOCK_RESPONSES[scenario];

    // 스트리밍 시뮬레이션
    setIsStreaming(true);
    const fullText = mockResponse.message || '';

    for (let i = 0; i <= fullText.length; i++) {
      await new Promise(resolve => setTimeout(resolve, 30));
      setStreamingText(fullText.slice(0, i));
    }

    setIsStreaming(false);
    setStreamingText('');

    return {
      message: fullText,
      meta: mockResponse.meta as WebChatResponse['meta'],
      suggestions: mockResponse.suggestions,
      showLeadForm: mockResponse.showLeadForm,
    };
  }, []);

  return { messages, sendMessage, isStreaming, streamingText };
}
```

#### Mock ↔ 실제 API 전환

```typescript
// src/features/website-chatbot/hooks/useChatbot.ts

import { useChatbotMock } from './useChatbot.mock';
import { useChatbotReal } from './useChatbot.real';

// 환경변수로 Mock/Real 전환
const USE_MOCK = import.meta.env.VITE_USE_MOCK_CHATBOT === 'true';

export const useChatbot = USE_MOCK ? useChatbotMock : useChatbotReal;
```

```bash
# .env.development (A가 UI 개발 시)
VITE_USE_MOCK_CHATBOT=true

# .env.production (실제 배포 시)
VITE_USE_MOCK_CHATBOT=false
```

#### B가 EF 테스트하는 curl 명령어

```bash
# 웹사이트 챗봇 EF 테스트 (비인증)

# 기본 메시지 전송
curl -X POST "https://bdrvowacecxnraaivlhr.supabase.co/functions/v1/retail-chatbot" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $(cat .env | grep SUPABASE_ANON_KEY | cut -d'=' -f2)" \
  -d '{
    "message": "매장 전환율을 올리려면 어떻게 해야 하나요?",
    "sessionId": "test-session-001",
    "turnCount": 1
  }'

# SSE 스트리밍 테스트
curl -N -X POST "https://bdrvowacecxnraaivlhr.supabase.co/functions/v1/retail-chatbot" \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -H "Authorization: Bearer $(cat .env | grep SUPABASE_ANON_KEY | cut -d'=' -f2)" \
  -d '{
    "message": "매장 레이아웃 최적화 방법 알려줘",
    "sessionId": "test-session-002",
    "turnCount": 3
  }'
```

### 4.6 충돌 발생 가능 지점

| # | 파일/영역 | 충돌 원인 | 해결 방법 |
|:--|:---|:---|:---|
| 1 | `website.types.ts` | A, B 모두 타입 수정 | B가 소유권 보유, 변경 시 A에게 Slack 알림 |
| 2 | `useChatbot.ts` | A가 호출부 수정, B가 구현부 수정 | 인터페이스(함수 시그니처) 먼저 합의 후 각자 작업 |
| 3 | `package.json` | A가 UI 라이브러리, B가 API 라이브러리 추가 | 동시 수정 금지, Slack 조율 후 순차 머지 |
| 4 | `src/pages/Index.tsx` | A가 챗봇 위젯 통합 시 수정 | A만 수정 가능, B는 EF만 담당 |

### 4.7 연동 체크포인트 (Week 4-7)

| 시점 | 체크 항목 | 담당 | 완료 조건 |
|:---|:---|:---|:---|
| Week 4 화요일 | SSE 스트리밍 연동 | A + B | Mock 없이 실제 EF에서 스트리밍 수신 성공 |
| Week 4 금요일 | 기본 대화 흐름 | A + B | 3턴 대화가 UI에서 정상 표시 |
| Week 5 화요일 | 토픽 분류 표시 | A + B | 응답 메타데이터가 UI에 반영 |
| Week 5 금요일 | 추천 질문 클릭 | A + B | 추천 칩 클릭 → 메시지 전송 동작 |
| Week 6 화요일 | 리드 캡처 폼 | A + B | showLeadForm=true 시 폼 표시 + 저장 |
| Week 6 금요일 | 세일즈 브릿지 | A + B | 5턴 이상 대화 시 솔루션 언급 확인 |
| Week 7 화요일 | E2E 테스트 | A + B | 전체 시나리오 3개 통과 |
| Week 7 금요일 | QA 버그 픽스 | A + B | P0 버그 0개 |

---

## 5. A + C OS 챗봇 동시 작업

### 5.1 경계선 다이어그램

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    OS 챗봇 (AI Assistant) 아키텍처                       │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │                     🟦 A 영역 (UI/UX)                        │       │
│  │                                                             │       │
│  │  src/features/ai-assistant/                                 │       │
│  │  ├── AssistantPanel.tsx      (사이드 패널)                   │       │
│  │  ├── AssistantButton.tsx     (열기 버튼)                     │       │
│  │  └── components/                                            │       │
│  │      ├── InlineChart.tsx     (미니 차트)                     │       │
│  │      ├── ActionButtons.tsx   (액션 버튼)                     │       │
│  │      └── QuickActions.tsx    (빠른 실행)                     │       │
│  │                                                             │       │
│  │  [A가 렌더링하고 실행하는 UIAction들]                         │       │
│  │  navigate() ─────────────────────────────────→ 페이지 이동   │       │
│  │  toggleLayer() ──────────────────────────────→ 레이어 제어   │       │
│  │  selectZone() ───────────────────────────────→ 존 선택      │       │
│  │  runSimulation() ────────────────────────────→ 시뮬 실행    │       │
│  └──────────────────────────────────────────────────────────────┘       │
│                                 │                                       │
│  ════════════════════════════════════════════════════════════════       │
│                     🟪 계약 경계선 (인터페이스)                          │
│                                 │                                       │
│     OSAssistantRequest ────────→│                                       │
│     OSAssistantResponse ←───────│ (actions 배열 포함)                   │
│     UIAction[] ←────────────────│                                       │
│  ════════════════════════════════════════════════════════════════       │
│                                 │                                       │
│  ┌──────────────────────────────│───────────────────────────────┐       │
│  │                     🟧 C 영역 (Backend)                      │       │
│  │                              │                               │       │
│  │  src/features/ai-assistant/                                  │       │
│  │  ├── hooks/                                                  │       │
│  │  │   ├── useAssistant.ts ───┘ (API 호출 + 상태 관리)         │       │
│  │  │   └── useActionDispatch.ts (UIAction 실행 디스패처)       │       │
│  │  ├── providers/                                              │       │
│  │  │   └── AssistantProvider.tsx (컨텍스트 수집)               │       │
│  │  ├── store/                                                  │       │
│  │  │   └── assistantStore.ts (신규 Zustand store)              │       │
│  │  └── types/                                                  │       │
│  │      └── assistant.types.ts                                  │       │
│  │                                                              │       │
│  │  supabase/functions/neuraltwin-assistant/                    │       │
│  │  ├── index.ts                                                │       │
│  │  ├── intent/classifier.ts, entityExtractor.ts, patterns.ts   │       │
│  │  ├── actions/queryActions.ts, aiActions.ts, uiActions.ts     │       │
│  │  ├── orchestrator/functionCaller.ts                          │       │
│  │  └── response/generator.ts, suggester.ts                     │       │
│  └──────────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.2 A+C의 특수 과제: UIAction 연결

OS 챗봇은 웹사이트 챗봇과 달리, **AI가 반환한 액션을 UI가 실제로 실행**해야 합니다.

```
C (Backend)                    A (UI)
┌──────────────┐              ┌──────────────┐
│              │              │              │
│ AI 응답 생성  │──actions──→│ 액션 디스패처  │──→ 페이지 이동
│              │  배열       │              │──→ 레이어 토글
│ "시뮬레이션   │              │ useAction   │──→ 존 선택
│  실행할게요"  │              │ Dispatch()  │──→ 시뮬 실행
│              │              │              │
└──────────────┘              └──────────────┘
```

### 5.3 UIAction 타입 계약서

```typescript
// src/features/ai-assistant/types/assistant.types.ts

// OS 챗봇 요청
export interface OSAssistantRequest {
  message: string;
  conversationId?: string;
  context: AssistantContext;
  settings?: {
    language: 'ko' | 'en';
    detailLevel: 'brief' | 'detailed';
  };
}

// 컨텍스트 (현재 UI 상태)
export interface AssistantContext {
  page: {
    current: 'dashboard' | 'product' | 'pricing' | 'contact' | 'profile';
    tab?: string;
  };
  selection?: {
    zoneIds?: string[];
    furnitureIds?: string[];
  };
  dateRange?: {
    start: string;
    end: string;
  };
  store: {
    id: string;
    name: string;
  };
  recentResults?: {
    hasSimulation: boolean;
    hasOptimization: boolean;
  };
}

// OS 챗봇 응답
export interface OSAssistantResponse {
  message: {
    content: string;
    format: 'text' | 'markdown';
  };
  actions?: UIAction[];
  data?: {
    type: 'kpi' | 'chart' | 'table' | 'simulation' | 'optimization';
    payload: unknown;
  };
  visualization?: {
    type: 'mini-chart' | 'progress' | 'comparison';
    config: unknown;
  };
  suggestions?: string[];
  meta: {
    conversationId: string;
    intent: string;
    confidence: number;
    executionTime: number;
  };
}

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// UIAction 타입 정의 (C가 반환, A가 실행)
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

export type UIAction =
  | NavigateAction
  | ToggleLayerAction
  | SelectZoneAction
  | RunSimulationAction
  | RunOptimizationAction
  | UpdateDateRangeAction
  | CameraMoveAction;

// 1. 페이지 이동
export interface NavigateAction {
  type: 'navigate';
  target: string;  // AVAILABLE_ROUTES 중 하나
  params?: Record<string, string>;
}

// 2. 레이어 토글 (3D 뷰어용)
export interface ToggleLayerAction {
  type: 'toggle_layer';
  layerId: string;
  visible: boolean;
}

// 3. 존 선택
export interface SelectZoneAction {
  type: 'select_zone';
  zoneId: string;
}

// 4. 시뮬레이션 실행
export interface RunSimulationAction {
  type: 'run_simulation';
  options: {
    scenario?: string;
    dateRange?: { start: string; end: string };
  };
}

// 5. 최적화 실행
export interface RunOptimizationAction {
  type: 'run_optimization';
  options: {
    type: 'furniture' | 'product' | 'staffing';
  };
}

// 6. 날짜 범위 변경
export interface UpdateDateRangeAction {
  type: 'update_date_range';
  start: string;
  end: string;
}

// 7. 카메라 이동 (3D 뷰어용)
export interface CameraMoveAction {
  type: 'camera_move';
  target: { x: number; y: number; z: number };
}
```

### 5.4 현재 라우팅 구조 (App.tsx 기반)

```typescript
// src/App.tsx에서 확인된 라우트 목록

export const AVAILABLE_ROUTES = [
  '/',           // Index (홈)
  '/product',    // Product (제품 소개)
  '/auth',       // Auth (로그인/회원가입)
  '/pricing',    // Pricing (가격)
  '/contact',    // Contact (문의)
  '/subscribe',  // Subscribe (구독)
  '/dashboard',  // Dashboard (대시보드)
  '/profile',    // Profile (프로필)
  '/privacy',    // Privacy (개인정보처리방침)
  '/terms',      // Terms (이용약관)
] as const;

export type AvailableRoute = typeof AVAILABLE_ROUTES[number];
```

### 5.5 useActionDispatch 구현

현재 프로젝트에 Zustand store가 없으므로, OS 챗봇용으로 신규 생성합니다.

```typescript
// src/features/ai-assistant/store/assistantStore.ts (신규 생성)

import { create } from 'zustand';

interface AssistantState {
  // 패널 상태
  isPanelOpen: boolean;
  panelPosition: 'right' | 'bottom' | 'floating';

  // 선택 상태
  selectedZoneId: string | null;
  selectedFurnitureIds: string[];

  // 날짜 범위
  dateRange: { start: string; end: string } | null;

  // 레이어 상태 (3D 뷰어용 - 향후 확장)
  visibleLayers: Record<string, boolean>;

  // 액션
  togglePanel: () => void;
  setPanelPosition: (pos: 'right' | 'bottom' | 'floating') => void;
  selectZone: (zoneId: string | null) => void;
  selectFurniture: (furnitureIds: string[]) => void;
  setDateRange: (range: { start: string; end: string } | null) => void;
  toggleLayer: (layerId: string, visible: boolean) => void;
}

export const useAssistantStore = create<AssistantState>((set) => ({
  // 초기 상태
  isPanelOpen: false,
  panelPosition: 'right',
  selectedZoneId: null,
  selectedFurnitureIds: [],
  dateRange: null,
  visibleLayers: {},

  // 액션
  togglePanel: () => set((state) => ({ isPanelOpen: !state.isPanelOpen })),
  setPanelPosition: (pos) => set({ panelPosition: pos }),
  selectZone: (zoneId) => set({ selectedZoneId: zoneId }),
  selectFurniture: (furnitureIds) => set({ selectedFurnitureIds: furnitureIds }),
  setDateRange: (range) => set({ dateRange: range }),
  toggleLayer: (layerId, visible) => set((state) => ({
    visibleLayers: { ...state.visibleLayers, [layerId]: visible }
  })),
}));
```

```typescript
// src/features/ai-assistant/hooks/useActionDispatch.ts (신규 생성)

import { useCallback } from 'react';
import { useNavigate } from 'react-router-dom';
import { useAssistantStore } from '../store/assistantStore';
import type { UIAction, AvailableRoute } from '../types/assistant.types';
import { AVAILABLE_ROUTES } from '../types/assistant.types';

/**
 * UIAction을 실제 UI 동작으로 변환하는 디스패처
 *
 * 사용법:
 *   const dispatch = useActionDispatch();
 *   dispatch(action); // 단일 액션
 *   actions.forEach(dispatch); // 다중 액션
 */
export function useActionDispatch() {
  const navigate = useNavigate();
  const {
    selectZone,
    setDateRange,
    toggleLayer
  } = useAssistantStore();

  const dispatch = useCallback((action: UIAction) => {
    switch (action.type) {
      // ───────────────────────────────────────────────────────
      // 1. 페이지 이동
      // ───────────────────────────────────────────────────────
      case 'navigate': {
        const target = action.target as AvailableRoute;
        if (AVAILABLE_ROUTES.includes(target)) {
          const queryParams = action.params
            ? '?' + new URLSearchParams(action.params).toString()
            : '';
          navigate(target + queryParams);
        } else {
          console.warn(`[ActionDispatch] Unknown route: ${action.target}`);
        }
        break;
      }

      // ───────────────────────────────────────────────────────
      // 2. 레이어 토글 (3D 뷰어용)
      // ───────────────────────────────────────────────────────
      case 'toggle_layer': {
        toggleLayer(action.layerId, action.visible);
        // TODO: 실제 3D 뷰어와 연동 시 추가 로직 필요
        console.log(`[ActionDispatch] Layer ${action.layerId} → ${action.visible}`);
        break;
      }

      // ───────────────────────────────────────────────────────
      // 3. 존 선택
      // ───────────────────────────────────────────────────────
      case 'select_zone': {
        selectZone(action.zoneId);
        // TODO: 3D 뷰어에서 해당 존 하이라이트
        console.log(`[ActionDispatch] Zone selected: ${action.zoneId}`);
        break;
      }

      // ───────────────────────────────────────────────────────
      // 4. 시뮬레이션 실행
      // ───────────────────────────────────────────────────────
      case 'run_simulation': {
        // TODO: 기존 시뮬레이션 기능과 연동
        // 현재는 시뮬레이션 페이지가 없으므로 대시보드로 이동 후 알림
        console.log('[ActionDispatch] Run simulation:', action.options);
        navigate('/dashboard');
        // toast.info('시뮬레이션 기능은 준비 중입니다.');
        break;
      }

      // ───────────────────────────────────────────────────────
      // 5. 최적화 실행
      // ───────────────────────────────────────────────────────
      case 'run_optimization': {
        // TODO: 기존 최적화 기능과 연동
        console.log('[ActionDispatch] Run optimization:', action.options);
        navigate('/dashboard');
        // toast.info('최적화 기능은 준비 중입니다.');
        break;
      }

      // ───────────────────────────────────────────────────────
      // 6. 날짜 범위 변경
      // ───────────────────────────────────────────────────────
      case 'update_date_range': {
        setDateRange({ start: action.start, end: action.end });
        console.log(`[ActionDispatch] Date range: ${action.start} ~ ${action.end}`);
        break;
      }

      // ───────────────────────────────────────────────────────
      // 7. 카메라 이동 (3D 뷰어용)
      // ───────────────────────────────────────────────────────
      case 'camera_move': {
        // TODO: 3D 뷰어 카메라 제어와 연동
        console.log('[ActionDispatch] Camera move:', action.target);
        break;
      }

      default: {
        console.warn('[ActionDispatch] Unknown action type:', (action as UIAction).type);
      }
    }
  }, [navigate, selectZone, setDateRange, toggleLayer]);

  return dispatch;
}
```

### 5.6 Mock 기반 독립 개발

#### A가 사용할 Mock Provider

```typescript
// src/features/ai-assistant/providers/AssistantProvider.mock.tsx

import React, { createContext, useContext, useState, useCallback } from 'react';
import type { OSAssistantRequest, OSAssistantResponse, AssistantContext } from '../types/assistant.types';

// Mock 시나리오
const MOCK_SCENARIOS = {
  // 시나리오 1: KPI 조회
  kpi: {
    message: { content: '오늘 매출은 **₩12,450,000**입니다. 전일 대비 **+5.2%** 상승했어요.', format: 'markdown' as const },
    data: { type: 'kpi' as const, payload: { revenue: 12450000, change: 5.2 } },
    visualization: { type: 'mini-chart' as const, config: { data: [100, 105, 102, 108, 112] } },
    suggestions: ['이번 주 트렌드 보여줘', '시간대별 매출 분석', '전환율은 어때?'],
    meta: { conversationId: 'mock-os-001', intent: 'query_kpi', confidence: 0.95, executionTime: 320 },
  },

  // 시나리오 2: 시뮬레이션 실행
  simulation: {
    message: { content: '크리스마스 시나리오로 시뮬레이션을 시작합니다. 완료되면 알려드릴게요!', format: 'text' as const },
    actions: [
      { type: 'run_simulation' as const, options: { scenario: 'christmas' } }
    ],
    suggestions: ['최적화도 같이 돌려줘', '결과 비교해서 보여줘'],
    meta: { conversationId: 'mock-os-002', intent: 'run_simulation', confidence: 0.92, executionTime: 150 },
  },

  // 시나리오 3: 존 분석
  zone: {
    message: { content: '입구 존의 전환율은 **23%**로, 매장 평균(18%)보다 높습니다. 체류시간은 평균 2분 30초입니다.', format: 'markdown' as const },
    actions: [
      { type: 'select_zone' as const, zoneId: 'zone-entrance' },
      { type: 'navigate' as const, target: '/dashboard' }
    ],
    data: { type: 'chart' as const, payload: { zoneId: 'zone-entrance', conversionRate: 23, dwellTime: 150 } },
    suggestions: ['다른 존과 비교해줘', '히트맵 보여줘', '동선 분석'],
    meta: { conversationId: 'mock-os-003', intent: 'query_zone', confidence: 0.88, executionTime: 450 },
  },
};

interface MockContextType {
  sendMessage: (request: OSAssistantRequest) => Promise<OSAssistantResponse>;
  context: AssistantContext;
  isLoading: boolean;
}

const MockContext = createContext<MockContextType | null>(null);

export function AssistantProviderMock({ children }: { children: React.ReactNode }) {
  const [isLoading, setIsLoading] = useState(false);

  const context: AssistantContext = {
    page: { current: 'dashboard' },
    store: { id: 'store-001', name: '테스트 매장' },
    recentResults: { hasSimulation: false, hasOptimization: false },
  };

  const sendMessage = useCallback(async (request: OSAssistantRequest): Promise<OSAssistantResponse> => {
    setIsLoading(true);

    // 메시지 기반 시나리오 선택
    await new Promise(resolve => setTimeout(resolve, 800)); // 지연 시뮬레이션

    let scenario: keyof typeof MOCK_SCENARIOS = 'kpi';
    if (request.message.includes('시뮬레이션') || request.message.includes('돌려')) {
      scenario = 'simulation';
    } else if (request.message.includes('존') || request.message.includes('입구')) {
      scenario = 'zone';
    }

    setIsLoading(false);
    return MOCK_SCENARIOS[scenario] as OSAssistantResponse;
  }, []);

  return (
    <MockContext.Provider value={{ sendMessage, context, isLoading }}>
      {children}
    </MockContext.Provider>
  );
}

export const useAssistantMock = () => {
  const ctx = useContext(MockContext);
  if (!ctx) throw new Error('useAssistantMock must be used within AssistantProviderMock');
  return ctx;
};
```

#### C가 EF 테스트하는 curl 명령어

```bash
# OS 챗봇 EF 테스트 (인증 필요)

# 먼저 JWT 토큰 획득 (Supabase Auth)
TOKEN=$(curl -s -X POST "https://bdrvowacecxnraaivlhr.supabase.co/auth/v1/token?grant_type=password" \
  -H "apikey: $(cat .env | grep SUPABASE_ANON_KEY | cut -d'=' -f2)" \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "testpassword"}' \
  | jq -r '.access_token')

# KPI 조회 테스트
curl -X POST "https://bdrvowacecxnraaivlhr.supabase.co/functions/v1/neuraltwin-assistant" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "message": "오늘 매출 얼마야?",
    "context": {
      "page": { "current": "dashboard" },
      "store": { "id": "store-001", "name": "테스트 매장" }
    }
  }'

# 시뮬레이션 실행 테스트
curl -X POST "https://bdrvowacecxnraaivlhr.supabase.co/functions/v1/neuraltwin-assistant" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "message": "크리스마스 시뮬레이션 돌려줘",
    "context": {
      "page": { "current": "dashboard" },
      "store": { "id": "store-001", "name": "테스트 매장" }
    }
  }'

# 존 분석 테스트
curl -X POST "https://bdrvowacecxnraaivlhr.supabase.co/functions/v1/neuraltwin-assistant" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "message": "입구 존 성과 어때?",
    "context": {
      "page": { "current": "dashboard" },
      "selection": { "zoneIds": ["zone-entrance"] },
      "store": { "id": "store-001", "name": "테스트 매장" }
    }
  }'
```

### 5.7 충돌 발생 가능 지점

| # | 파일/영역 | 충돌 원인 | 해결 방법 |
|:--|:---|:---|:---|
| 1 | `assistant.types.ts` | UIAction 타입 추가 시 A+C 모두 수정 | UIAction 추가 프로토콜 준수 (아래 5.8 참조) |
| 2 | `useActionDispatch.ts` | C가 새 액션 추가, A가 렌더링 로직 수정 | C가 먼저 타입+디스패처 추가 → A가 UI 구현 |
| 3 | `AssistantPanel.tsx` | A가 레이아웃, C가 Provider 연동 | A가 UI 컨테이너 소유, C는 Provider/Hook만 담당 |
| 4 | `assistantStore.ts` | C가 상태 추가, A가 상태 사용 | C가 store 소유, 변경 시 A에게 알림 |

### 5.8 UIAction 추가 프로토콜

새로운 UIAction을 추가할 때는 반드시 아래 순서를 따릅니다:

```
┌─────────────────────────────────────────────────────────────────────────┐
│  UIAction 추가 프로토콜 (A+C 공동)                                       │
│                                                                         │
│  Step 1: C가 Slack에 신규 액션 제안                                      │
│          → 타입 정의 초안 공유                                           │
│                                                                         │
│  Step 2: A가 UI 구현 가능 여부 확인 (30분 내 회신)                        │
│          → "현재 라우트/기능으로 가능" 또는 "추가 개발 필요"              │
│                                                                         │
│  Step 3: 합의 후 C가 구현                                                │
│          1) assistant.types.ts에 타입 추가                               │
│          2) useActionDispatch.ts에 case 추가                            │
│          3) EF에서 해당 액션 반환 로직 구현                              │
│                                                                         │
│  Step 4: A가 UI에서 액션 버튼/결과 렌더링                                 │
│          → ActionButtons.tsx에 버튼 추가                                 │
│                                                                         │
│  Step 5: A+C 공동 테스트                                                 │
└─────────────────────────────────────────────────────────────────────────┘
```

### 5.9 연동 체크포인트 (Week 7-10)

| 시점 | 체크 항목 | 담당 | 완료 조건 |
|:---|:---|:---|:---|
| Week 7 화요일 | 기본 응답 연동 | A + C | EF 응답이 패널에 표시 |
| Week 7 금요일 | 인텐트 분류 테스트 | A + C | 3가지 인텐트 정확 분류 |
| Week 8 화요일 | KPI 조회 + 미니차트 | A + C | 데이터 → 차트 렌더링 |
| Week 8 금요일 | navigate 액션 | A + C | AI 응답 → 페이지 이동 |
| Week 9 화요일 | 시뮬레이션 호출 | A + C | run_simulation → 기존 EF 연동 |
| Week 9 금요일 | 컨텍스트 수집 | A + C | 페이지/선택 상태가 EF로 전달 |
| Week 10 화요일 | E2E 테스트 | A + C | 전체 시나리오 5개 통과 |
| Week 10 금요일 | QA 버그 픽스 | A + C | P0 버그 0개 |

---

## 6. 인터페이스 계약 기반 병렬 개발

### 6.1 계약 파일 목록

| 파일 | 위치 | 소유 | 내용 |
|:---|:---|:---|:---|
| `chat.types.ts` | `src/shared/chat/types/` | 🟪 전원 | 공통 타입 (ChatMessage, ChatSession 등) |
| `website.types.ts` | `src/features/website-chatbot/types/` | 🟩 B | 웹 챗봇 요청/응답 타입 |
| `assistant.types.ts` | `src/features/ai-assistant/types/` | 🟧 C | OS 챗봇 요청/응답 + UIAction 타입 |

### 6.2 계약 파일 Lock 규칙

```
┌─────────────────────────────────────────────────────────────────────────┐
│  계약 파일 수정 규칙                                                     │
│                                                                         │
│  1. 수정 전 Slack #chatbot-dev 채널에 알림                               │
│     → "🔒 [website.types.ts] WebChatResponse에 필드 추가 예정"           │
│                                                                         │
│  2. 30분 내 이의 없으면 수정 진행                                         │
│                                                                         │
│  3. 수정 후 PR에 '[CONTRACT]' 태그 필수                                  │
│     → 예: "[CONTRACT] WebChatResponse에 tokenCount 필드 추가"            │
│                                                                         │
│  4. 해당 타입을 사용하는 다른 개발자가 자신의 코드 업데이트                  │
│     → B가 타입 변경 → A가 UI 코드 수정                                   │
│                                                                         │
│  ⚠️ 예외: Breaking Change (기존 필드 삭제/타입 변경) 시                   │
│     → 반드시 전원 동의 후 진행                                           │
│     → 마이그레이션 가이드 PR 설명에 포함                                  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 6.3 계약 변경 프로토콜

```
Step 1: Slack 알림
────────────────────────────────────────────────────────
@channel 🔒 [CONTRACT 변경 제안]

파일: src/features/ai-assistant/types/assistant.types.ts
변경: UIAction에 'show_notification' 타입 추가
이유: AI가 토스트 알림을 표시할 수 있도록

interface ShowNotificationAction {
  type: 'show_notification';
  title: string;
  message: string;
  variant: 'info' | 'success' | 'warning' | 'error';
}

영향 범위:
- A: ActionButtons.tsx에서 알림 버튼 추가 필요
- C: useActionDispatch.ts에 case 추가 필요

30분 내 이의 없으면 진행합니다.
────────────────────────────────────────────────────────

Step 2: 30분 대기 (이의 있으면 논의)

Step 3: 합의 후 커밋
  git commit -m "contract(assistant): add ShowNotificationAction type"

Step 4: A, C 각자 자신의 코드 업데이트
```

### 6.4 SSE 스트리밍 이벤트 계약 (웹 챗봇 전용)

```typescript
// SSE 이벤트 타입 정의
// supabase/functions/_shared/chatTypes.ts

export type SSEEventType =
  | 'chunk'       // 텍스트 청크 (스트리밍 중)
  | 'done'        // 스트리밍 완료
  | 'meta'        // 메타데이터
  | 'suggestions' // 추천 질문
  | 'error';      // 에러

export interface SSEEvent {
  event: SSEEventType;
  data: string; // JSON 문자열
}

// 이벤트별 data 페이로드
export interface ChunkEventData {
  text: string;
}

export interface DoneEventData {
  message: string;
}

export interface MetaEventData {
  conversationId: string;
  topicCategory: string;
  painPoint?: string;
  sentiment: 'positive' | 'neutral' | 'negative';
  solutionMentioned: boolean;
}

export interface SuggestionsEventData {
  suggestions: string[];
  showLeadForm?: boolean;
}

export interface ErrorEventData {
  code: string;
  message: string;
}
```

**SSE 전송 예시 (B가 구현):**

```typescript
// supabase/functions/retail-chatbot/index.ts

function sendSSE(writer: WritableStreamDefaultWriter, event: SSEEventType, data: object) {
  const encoder = new TextEncoder();
  writer.write(encoder.encode(`event: ${event}\ndata: ${JSON.stringify(data)}\n\n`));
}

// 스트리밍 응답
sendSSE(writer, 'chunk', { text: '안녕하세요!' });
sendSSE(writer, 'chunk', { text: ' 리테일 전문' });
sendSSE(writer, 'chunk', { text: ' AI입니다.' });
sendSSE(writer, 'done', { message: '안녕하세요! 리테일 전문 AI입니다.' });
sendSSE(writer, 'meta', { conversationId: 'xxx', topicCategory: 'general', sentiment: 'neutral', solutionMentioned: false });
sendSSE(writer, 'suggestions', { suggestions: ['전환율 개선', '레이아웃 최적화'] });
```

**SSE 파싱 예시 (A가 구현):**

```typescript
// src/shared/chat/hooks/useStreaming.ts

export function useStreaming() {
  const parseSSE = useCallback((rawData: string) => {
    const lines = rawData.split('\n');
    let currentEvent = '';
    let currentData = '';

    for (const line of lines) {
      if (line.startsWith('event: ')) {
        currentEvent = line.slice(7);
      } else if (line.startsWith('data: ')) {
        currentData = line.slice(6);

        if (currentEvent && currentData) {
          const parsed = JSON.parse(currentData);

          switch (currentEvent) {
            case 'chunk':
              // 텍스트 누적
              break;
            case 'done':
              // 스트리밍 완료 처리
              break;
            case 'meta':
              // 메타데이터 저장
              break;
            case 'suggestions':
              // 추천 질문 표시
              break;
            case 'error':
              // 에러 처리
              break;
          }
        }
      }
    }
  }, []);

  return { parseSSE };
}
```

---

## 7. PR 워크플로우 & 코드 리뷰

### 7.1 PR 템플릿

`.github/pull_request_template.md` 파일:

```markdown
## 변경 유형

<!-- 해당하는 항목에 체크하세요 -->

- [ ] 🟦 UI/UX (프론트엔드 컴포넌트, 스타일)
- [ ] 🟩 Web Bot Backend (retail-chatbot EF, 웹 챗봇 훅)
- [ ] 🟧 OS Bot Backend (neuraltwin-assistant EF, OS 챗봇 훅)
- [ ] 🟪 공유 영역 (계약 타입, DB 스키마, 공유 유틸)
- [ ] ⬜ 기타 (문서, 설정)

## 변경 내용

<!-- 무엇을 변경했는지 간략히 설명하세요 -->

## 인터페이스 변경 여부

- [ ] ✅ 인터페이스 변경 **없음**
- [ ] ⚠️ 인터페이스 변경 **있음** → 아래 상세 기술

<!-- 인터페이스 변경 시 작성 -->
<details>
<summary>인터페이스 변경 상세</summary>

**변경된 파일:**
- `src/.../.../types.ts`

**변경 내용:**
```typescript
// Before
interface Foo { ... }

// After
interface Foo { newField: string; ... }
```

**영향 받는 코드:**
- `src/.../Component.tsx` - A 업데이트 필요
- `supabase/functions/.../index.ts` - B/C 업데이트 필요

</details>

## 테스트 체크리스트

- [ ] 로컬에서 `npm run build` 성공
- [ ] 로컬에서 `npm run lint` 통과
- [ ] 관련 기능 수동 테스트 완료
- [ ] (EF 변경 시) curl 테스트 완료
- [ ] (UI 변경 시) 모바일 반응형 확인

## 스크린샷 / 데모

<!-- UI 변경 시 스크린샷 첨부 -->
<!-- EF 변경 시 curl 응답 예시 첨부 -->

## 관련 이슈

<!-- 관련 이슈 번호가 있으면 링크 -->
Closes #

## 리뷰어 체크

<!-- CODEOWNERS 기반으로 자동 할당됩니다. 추가 리뷰어가 필요하면 수동 지정하세요. -->

---

https://claude.ai/code
```

### 7.2 리뷰 매트릭스

| 변경 영역 | 필수 리뷰어 | Approve 필요 수 |
|:---|:---|:---|
| 🟦 UI 컴포넌트 | A | 1 (자체) |
| 🟩 Web Bot EF | B | 1 (자체) |
| 🟧 OS Bot EF | C | 1 (자체) |
| 🟪 계약 타입 | 전원 | 2 (합의) |
| 🟪 DB 스키마 | B + C | 2 |
| 🟪 package.json | 전원 | 2 |
| src/App.tsx | A + 1명 | 2 |
| 기존 컴포넌트 수정 | A + 1명 | 2 |

### 7.3 긴급 Hotfix 프로세스

```
┌─────────────────────────────────────────────────────────────────────────┐
│  긴급 Hotfix 프로세스                                                    │
│                                                                         │
│  1. Slack #chatbot-dev에 🚨 알림                                        │
│     → "🚨 [HOTFIX] 웹 챗봇 EF 500 에러 발생"                            │
│                                                                         │
│  2. hotfix/issue-number 브랜치 생성 (main에서)                          │
│     git checkout main && git checkout -b hotfix/issue-123               │
│                                                                         │
│  3. 최소 변경으로 수정 후 PR                                             │
│     → 제목에 [HOTFIX] 태그                                              │
│     → 1명 리뷰로 Approve 가능                                           │
│                                                                         │
│  4. main에 바로 머지 (Squash Merge)                                     │
│                                                                         │
│  5. develop에도 cherry-pick                                             │
│     git checkout develop && git cherry-pick <commit-hash>               │
│                                                                         │
│  6. 사후 회고 (다음 스탠드업)                                            │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 8. 충돌 방지 실전 규칙

### 8.1 동시 수정 위험 파일 목록

현재 프로젝트에서 동시 수정 시 충돌 위험이 높은 파일들:

| 파일 | 위험도 | 수정 규칙 |
|:---|:---|:---|
| `package.json` | 🔴 높음 | 동시 수정 금지, Slack 조율 |
| `tsconfig.json` | 🔴 높음 | 동시 수정 금지, 변경 시 전원 알림 |
| `src/App.tsx` | 🔴 높음 | A만 수정, 라우트 추가 시 전원 알림 |
| `src/shared/chat/types/chat.types.ts` (신규) | 🔴 높음 | 전원 합의 후 수정 |
| `supabase/migrations/*.sql` (신규) | 🟡 중간 | B+C 공동 작업, 파일 분리 권장 |
| `src/hooks/useAuth.ts` | 🟡 중간 | 수정 최소화, 변경 시 알림 |
| `tailwind.config.js` | 🟡 중간 | 동시 수정 금지 |
| `vite.config.ts` | 🟡 중간 | 동시 수정 금지 |

### 8.2 매일 아침 Rebase 리듬

```bash
# 매일 아침 작업 시작 전 (필수!)

# 1. 현재 작업 저장
git stash

# 2. develop 최신화
git fetch origin develop

# 3. 현재 브랜치에 rebase
git rebase origin/develop

# 4. 충돌 있으면 해결 후
git add .
git rebase --continue

# 5. 작업 복원
git stash pop
```

### 8.3 커밋 메시지 컨벤션

현재 프로젝트의 커밋 메시지는 혼재되어 있습니다 (예: "feat: ...", "Changes", "마크다운 파일 저장").
챗봇 개발부터 새로운 컨벤션을 적용합니다:

```
<type>(<scope>): <subject>

type:
  feat     새 기능
  fix      버그 수정
  docs     문서
  style    코드 스타일 (로직 변경 없음)
  refactor 리팩토링
  test     테스트
  chore    빌드/설정

scope:
  web-ui   웹 챗봇 UI
  web-ef   웹 챗봇 Edge Function
  os-ui    OS 챗봇 UI
  os-ef    OS 챗봇 Edge Function
  shared   공유 컴포넌트/유틸
  contract 인터페이스 계약
  schema   DB 스키마

예시:
  feat(shared): Chat UI Kit 기본 컴포넌트 구현
  feat(web-ef): Claude API 연동 및 스트리밍 구현
  feat(os-ui): AssistantPanel 레이아웃 구현
  fix(web-ui): 모바일 위젯 위치 수정
  contract(web): WebChatResponse에 tokenCount 추가
  schema: chat_conversations 테이블 생성
```

---

## 9. 일일 동기화 체크리스트

### 9.1 매일 아침 스탠드업 (09:30, 15분)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Daily Standup Format                                                   │
│                                                                         │
│  각자 3가지 공유 (2분씩):                                                │
│                                                                         │
│  1. 어제 한 일                                                          │
│     → "ChatBubble 컴포넌트 완성, 스트리밍 파싱 테스트 중"                │
│                                                                         │
│  2. 오늘 할 일                                                          │
│     → "TypingIndicator 구현, useChatbot Mock 연동 테스트"               │
│                                                                         │
│  3. 블로커 / 다른 사람 필요한 것                                         │
│     → "B에게: SSE 이벤트 순서 확인 필요"                                 │
│     → "C에게: UIAction 타입 정의 검토 부탁"                              │
│                                                                         │
│  마무리 (3분):                                                          │
│  - 오늘 연동 테스트 일정 확인                                            │
│  - 계약 변경 예정 사항 공유                                              │
└─────────────────────────────────────────────────────────────────────────┘
```

### 9.2 A+B 연동 체크포인트 (Week 4-7)

| 요일 | 시간 | 내용 | 참석 |
|:---|:---|:---|:---|
| 화요일 | 14:00 | 연동 테스트 (1시간) | A + B |
| 금요일 | 14:00 | 연동 테스트 + 버그 리뷰 (1.5시간) | A + B |

**체크포인트 아젠다:**
1. Mock 없이 실제 EF 연동 테스트
2. SSE 스트리밍 정상 동작 확인
3. 발견된 버그 즉석 수정 또는 이슈 등록
4. 다음 체크포인트까지 목표 설정

### 9.3 A+C 연동 체크포인트 (Week 7-10)

| 요일 | 시간 | 내용 | 참석 |
|:---|:---|:---|:---|
| 화요일 | 15:00 | 연동 테스트 (1시간) | A + C |
| 금요일 | 15:30 | 연동 테스트 + 버그 리뷰 (1.5시간) | A + C |

**체크포인트 아젠다:**
1. UIAction 실행 테스트
2. 컨텍스트 전달 확인
3. 인텐트별 응답 검증
4. 새 UIAction 추가 논의

### 9.4 주간 금요일 통합 리뷰 (17:00, 1시간)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  Weekly Integration Review Agenda                                        │
│                                                                         │
│  1. 이번 주 성과 (15분)                                                  │
│     → 각자 데모 (있으면)                                                 │
│     → 완료된 체크포인트 확인                                             │
│                                                                         │
│  2. 다음 주 계획 (15분)                                                  │
│     → 개인별 태스크 확인                                                 │
│     → 연동 일정 조율                                                     │
│                                                                         │
│  3. 이슈 논의 (20분)                                                    │
│     → 발생한 충돌/블로커                                                 │
│     → 인터페이스 변경 필요 사항                                          │
│     → 기술적 의사결정 필요 사항                                          │
│                                                                         │
│  4. 코드 품질 리뷰 (10분)                                                │
│     → PR 리뷰 백로그 확인                                                │
│     → 기술 부채 논의                                                     │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 10. 부록: 빠른 참조 카드

### 10.1 A+B 웹사이트 챗봇 한눈에 보기

```
┌─────────────────────────────────────────────────────────────────────────┐
│  A + B 협업 요약 (웹사이트 챗봇)                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  🟦 A의 영역                        🟩 B의 영역                          │
│  ─────────────────────────────     ─────────────────────────────       │
│  src/shared/chat/components/       src/features/website-chatbot/        │
│  src/shared/chat/hooks/              hooks/useChatbot.ts               │
│  src/features/website-chatbot/       types/website.types.ts            │
│    *.tsx (UI 컴포넌트)             supabase/functions/retail-chatbot/   │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  ✅ 할 일                           ❌ 절대 하지 말 것                   │
│  ─────────────────────────────     ─────────────────────────────       │
│  A: Mock으로 UI 먼저 개발           A: useChatbot.ts 직접 수정           │
│  B: curl로 EF 독립 테스트           B: *.tsx 컴포넌트 수정               │
│  A+B: 화/금 연동 테스트             동시에 website.types.ts 수정        │
│  계약 변경 시 Slack 알림            package.json 동시 수정               │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  📡 연동 인터페이스                                                      │
│  ─────────────────────────────────────────────────────────────────────  │
│  Request:  WebChatRequest  (message, sessionId, turnCount)              │
│  Response: WebChatResponse (message, meta, suggestions, showLeadForm)   │
│  Stream:   SSE (chunk → done → meta → suggestions)                      │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  🔀 Mock 전환                                                           │
│  ─────────────────────────────────────────────────────────────────────  │
│  .env.development: VITE_USE_MOCK_CHATBOT=true   (A 개발 시)             │
│  .env.production:  VITE_USE_MOCK_CHATBOT=false  (배포 시)               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 10.2 A+C OS 챗봇 한눈에 보기

```
┌─────────────────────────────────────────────────────────────────────────┐
│  A + C 협업 요약 (OS 챗봇)                                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  🟦 A의 영역                        🟧 C의 영역                          │
│  ─────────────────────────────     ─────────────────────────────       │
│  src/features/ai-assistant/        src/features/ai-assistant/           │
│    AssistantPanel.tsx                hooks/useAssistant.ts             │
│    AssistantButton.tsx               hooks/useActionDispatch.ts        │
│    components/*.tsx                  providers/AssistantProvider.tsx   │
│                                      store/assistantStore.ts           │
│                                      types/assistant.types.ts          │
│                                    supabase/functions/                  │
│                                      neuraltwin-assistant/              │
│                                      _shared/                           │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  ✅ 할 일                           ❌ 절대 하지 말 것                   │
│  ─────────────────────────────     ─────────────────────────────       │
│  A: Mock Provider로 UI 개발         A: useActionDispatch.ts 직접 수정   │
│  C: curl로 EF + 인텐트 테스트       C: *.tsx 컴포넌트 수정               │
│  A+C: 화/금 연동 테스트             동시에 assistant.types.ts 수정      │
│  UIAction 추가 시 프로토콜 준수     assistantStore.ts 동시 수정         │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  📡 연동 인터페이스                                                      │
│  ─────────────────────────────────────────────────────────────────────  │
│  Request:  OSAssistantRequest  (message, context, settings)             │
│  Response: OSAssistantResponse (message, actions[], data, suggestions)  │
│  Actions:  UIAction[] (navigate, toggle_layer, select_zone, ...)        │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  🎯 UIAction 추가 순서                                                   │
│  ─────────────────────────────────────────────────────────────────────  │
│  1. C가 Slack에 제안 → 2. A가 확인 → 3. C가 타입+디스패처 구현           │
│  → 4. A가 버튼/UI 구현 → 5. A+C 테스트                                   │
│                                                                         │
│  ─────────────────────────────────────────────────────────────────────  │
│                                                                         │
│  🗺️ 사용 가능한 라우트 (navigate 액션용)                                 │
│  ─────────────────────────────────────────────────────────────────────  │
│  /, /product, /auth, /pricing, /contact, /subscribe,                    │
│  /dashboard, /profile, /privacy, /terms                                 │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 문서 끝

**작성자**: Claude Code
**최종 수정**: 2026-02-05
**다음 리뷰**: Phase 0 완료 시 (Week 3)

---

*이 가이드는 NEURALTWIN 듀얼 챗봇 개발 기간 동안 지속적으로 업데이트됩니다. 변경 사항은 PR을 통해 관리하며, 모든 개발자가 최신 버전을 참조해야 합니다.*
